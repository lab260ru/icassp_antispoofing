# H8 idea angles

See `DEEPRESEARCH.md` for sources and `PLAN.md` for the locked experiment
matrix. The seed matrix covers rank-space representation, robust objective,
fixed aggregation, and exhaustive verification; no model subset, target label,
or prior H1--H7 result may select a candidate.
