# H5 supplementary concordance figure 001 — visual inspection

Date: 2026-08-10

The authoritative `figures_median_concordance_v2` PNG was inspected at high
detail after deterministic rendering.

- All three panel headings are readable and non-overlapping.
- The 28 feature rows, fixed five-corpus colorbar, six light-gray unavailable
  cells with `x` markers, and 17 green terminal-descriptive markers are legible.
- Legend text explicitly states that unavailable cells are not imputed and that
  green markers are not ranking/selection signals.
- Vector PDF and 300-DPI PNG hashes match the figure metadata. No scientific
  value, ordering, scale, marker definition, or input was changed after the
  display protocol was locked.

The preceding non-authoritative `figures_median_concordance` version is
preserved because its panel headings overlapped. Its layout defect was detected
visually before use; the corrected v2 layout is the sole recommended display.
