study:
  id: icassp_signal_audit_v1
  seed: 2609
  deadline_utc: "2026-08-10T18:03:00Z"
storage:
  hdd_root: /home/kirill/mnt/hdd_6tb_1/icassp_antispoofing
  dataset_root: /home/kirill/mnt/hdd_6tb_1/icassp_antispoofing/datasets
  model_root: /home/kirill/mnt/hdd_6tb_1/icassp_antispoofing/models
  run_root: /home/kirill/mnt/hdd_6tb_1/icassp_antispoofing/runs
datasets:
  discovery: [ASVspoof2019_LA, ASVspoof2021_LA, ASVspoof2021_DF]
  confirmation: [InTheWild, ASVspoof5]
  fallback: [DeepVoice, LibriSeVoc, XMAD, CFAD, CVoiceFake_small, DECRO]
  maximum_full_examples: 50000
  subset_per_label: 5000
  intervention_per_label: 500
models:
  score_panel: [Spectra-AASIST, AASIST, Res2TCNGuard, RawTFNet, WhisperMFCCMesoNet, W2V2-AASIST, XLSR-SLS, RawBMamba]
  intervention_panel: [Spectra-AASIST, AASIST, Res2TCNGuard, W2V2-AASIST]
  intervention_fallback: XLSR-SLS
features:
  version: v1_28
  views: [full_waveform, deterministic_crop, preemphasized_crop]
statistics:
  correction: benjamini-hochberg
  bootstrap_unit: speaker_or_source_utterance
  bootstrap_replicates: 2000
  portable_association: "same direction in >=4/5 core datasets, significant in both confirmation datasets, and present in >=5/8 models"
  causal_sensitivity: "bootstrap CI excludes zero, standardized paired score change >=0.2, and consistent direction in >=3 datasets and >=3/4 models"
training:
  precision: bf16
  seeds: [13, 37, 73]
  batch_sizes: [8, 16, 32, 64]
  num_workers: [4, 8, 12, 16]
  max_vram_fraction: 0.92
  early_stopping_patience: 2
