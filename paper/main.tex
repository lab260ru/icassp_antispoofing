% Periodicity, Not Length -- ICASSP 2026
% Build:  bash paper/build.sh
% Numbers: every experimental value is a macro defined in numbers.tex, which is
% generated by analysis/make_numbers.py. Never hand-edit a number here.
\documentclass{article}
\usepackage{spconf,amsmath,amssymb,amsthm,graphicx,booktabs,hyperref,microtype}
\usepackage[table]{xcolor}
\usepackage{cite}   % IEEE house style: [15]--[19] instead of [15, 16, 17, 18, 19]

% Display and theorem spacing, tightened. The page limit is hard and this costs
% no content; the defaults leave more air around one display equation and three
% theorem blocks than a four-page paper can afford.
\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}
\setlength{\abovedisplayshortskip}{2pt}
\setlength{\belowdisplayshortskip}{2pt}
% Theorem-environment spacing too: three of these plus an assumption block.
\makeatletter
\def\thm@space@setup{\thm@preskip=3pt \thm@postskip=3pt}
\makeatother
% Float separation. Two floats at 12pt of default padding above and below cost
% more lines than the last paragraph of the discussion is worth, and the page
% limit is hard. Caption spacing likewise.
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{4pt}
\setlength{\belowcaptionskip}{0pt}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{assumption}{Assumption}

\input{numbers}

\title{Periodicity, Not Length: Locating the Repetition-Counting\\
       Failure in Neural Text-to-Speech}

\twoauthors
  {Ivan Viakhirev$^{1}$, Kirill Borodin$^{2,3}$, Grach Mkrtchian$^{2}$}
  {$^{1}$ITMO University, Russia \\
   $^{2}$lab260, Moscow Technical University of Communications and Informatics, Russia \\
   $^{3}$BitmanagerAI, UAE}
  {}{}

\begin{document}
\maketitle

\begin{abstract}
Text-to-speech models loop, truncate and lose count on text that repeats a
phrase many times. We isolate what drives it with a parametric repetition
ladder: \NStimuli{} stimuli in which a target word recurs $k$ times, each paired
with a \emph{length-matched control} of the same carrier and word count whose
repeated token is replaced by distinct fillers. Across \NModels{} checkpoints
from \NFamilies{} architectures, models render repeated text exactly in
\ExactRep{}\% of generations against \ExactCtl{}\% for their controls at
$k\ge6$: a gap of \FamExact{} points $[\FamExactLo{},\FamExactHi{}]$ by
architecture family ($n{=}\FamExactN{}$), favouring the control in all
\CkExactN{} checkpoints, and \FullLadderGap{} points over the whole ladder from
$k{=}1$. The limit tracks \emph{periodicity, not length}. It survives greedy decoding, an
order-of-magnitude penalty sweep, never-cycled controls and every exclusion rule
switched off (\ExSensAll{} points), and replicates at
\IndGapLo{}--\IndGapHi{} points across \IndNJudges{} recognisers, ruling out
the judge-side artifact that would otherwise explain it. Measuring it needs care: the obvious judge, Whisper,
is autoregressive and biased \emph{against} the phenomenon, scoring
\WhisperValPer{} of the true count on periodic ground truth where a CTC
recogniser scores \CtcValPer{}. Nor is the failure peculiar to autoregressive
decoding --- of two non-autoregressive baselines one is immune, one is not.
A Lean-4-verified bound says that if the per-repetition state map contracts, no
Lipschitz readout can separate counts past a computable horizon. We measure that
premise with a validated estimator and reject it: $q=\JacQRep{}$, and repetition
makes the map \emph{more} expansive rather than less. The proposed mechanism is
refuted; the phenomenon is not.
\end{abstract}

\begin{keywords}
speech synthesis, hallucination, probing, autoregressive models,
formal verification
\end{keywords}

\section{Introduction}
\label{sec:intro}

Ask an autoregressive text-to-speech (TTS) model to say \emph{six hundred
sixty-six thousand six hundred sixty-six}, or to repeat a word a dozen times, and
it will frequently get the count wrong: it stops early, runs on, or locks into a
loop. The behaviour is documented as a stability
problem~\cite{song2024ellav,wang2023valle} and usually attributed to
exposure bias or to the absence of a monotonic alignment constraint, with fixes
proposed at the level of attention supervision or decoding
rules~\cite{xin2024ralle,du2024cosyvoice2} --- the speech analogue of neural text
degeneration~\cite{holtzman2020curious,welleck2020neural}.

Reports of this behaviour confound two things, because repeating a phrase $k$
times makes the text both \emph{longer} and \emph{periodic}. If long inputs are
simply harder --- through attention span, exposure bias or a duration budget ---
then nothing about repetition needs explaining. The manipulation here separates
them by construction: every repeated item is paired with a
\emph{length-matched control} of the same carrier and the same word count, in
which the $k$ copies of one word become $k$ non-adjacent distinct ones. A
length account predicts the two curves coincide. They do not, and that contrast
is the paper's sharpest result.

Measuring it is its own problem, because the natural instrument shares the
failure it is meant to detect: a judge biased in the direction of the effect can
manufacture it. That constrains the design rather than decorating it, and
Section~\ref{sec:method} treats it as a result in its own right.

We contribute, in order of what the evidence supports: a controlled manipulation
isolating periodicity from length across \NModels{} checkpoints and two
non-autoregressive baselines, on which the deficit tracks periodicity and
survives the field's standard mitigations; a judge methodology for repetitive
speech, the obvious judge shown disqualified; and a measurement closing off the
mechanism we proposed for it, the contraction premise of a Lean-verified bound
being not merely untested but false in these decoders. The work is a companion to a spectral study of hallucination in
encoder--decoder ASR~\cite{viakhirev2026dispersion}.

\section{Method}
\label{sec:method}

\noindent\textbf{Stimuli.}
A parametric repetition ladder of \NStimuli{} items: carrier sentences with a
target word repeated $k \in \{1,2,3,4,6,8,12,16,24,32\}$ times, plus repeated
sentences, tongue twisters and number phrases. Every item at $k\ge2$ has a
\emph{length-matched control}: same carrier and word count, the repeated token
replaced by fillers from a per-template pool of eight, cycled above $k\!=\!8$
(S10).

\noindent\textbf{Models.}
The panel is \NModels{} checkpoints but only \NFamilies{} designs, scale
variants making up the difference: Llasa at 1B/3B/8B (Llama backbones over X-codec2
tokens~\cite{ye2025llasa}) and Qwen3-TTS at 0.6B/1.7B are within-architecture
ladders, plus XTTS-v2~\cite{casanova2024xtts}. VITS~\cite{kim2021vits} and
F5-TTS~\cite{chen2024f5tts} run as non-autoregressive baselines, XTTS-v2 and
Qwen3-TTS-0.6B at altered repetition penalty as ablations; neither group is
pooled into the panel. Every arm renders every item at \NSeeds{} seeds.

\noindent\textbf{Behavioural measurement, and why the obvious judge is wrong.}
The natural instrument, Whisper large-v3~\cite{radford2023whisper}, is
demonstrably the wrong one: its decoder is autoregressive with a language-model
prior, so it is subject to the very dynamics under study. Audited against
concatenative audio with known ground truth its counted/true ratio falls to
$0.27$--$0.65$ over $k\ge4$ and to \WhisperValPer{} at the top of the ladder,
while \emph{same-voice distinct-word} sequences of identical length score
$1.00$: a bias specific to periodicity, pointing the way of the effect we want
to measure. We score instead with a CTC recogniser
(\texttt{wav2vec2-large-960h-lv60-self}, greedy best-path, no language model),
put through the same audit --- ratio \CtcValPer{} on the same ground truth: worse in absolute WER, right for this purpose.

$\hat c$ is obtained by matching the target unit against the transcript's word
sequence, counting non-overlapping occurrences in order and skipping
unrecognised ones rather than stopping at the first miss, which would void
credit for every later filler in a control. The reported quantity is the
\emph{relative count error} $(\hat c - k)/k$; exact-match scores $30$-of-$32$
and $3$-of-$32$ alike. Of \MainNGen{} panel generations we exclude
\MainExclTmpl{} in one template whose fillers the judge never transcribes,
\MainExclCap{} stopped by our token budget rather than by the model, and
\MainExclDegen{} degenerate, leaving \MainKept{}; S10 gives per-condition rates.

\noindent\textbf{State measurement.}
For each instrumented generation we record per-step hidden states at probe
layers and head-averaged attention onto the text span. \emph{Effective rank} is the
exponentiated entropy of the normalised singular-value spectrum; the count probe
is ridge regression on mean-pooled states, the layer chosen by
leave-one-template-out $R^2$ (S6).

\noindent\textbf{Code, data, settings.}
Lean development, stimuli, code, per-item tables, the full supplement and both
transcript sets are public at tag \texttt{v1.1-icassp} of
\url{https://github.com/lab260ru/icassp_antispoofing} (name predates this
study). \texttt{build.sh} regenerates every number here from pinned revisions
(S19), and a stratified \SampleN{}-clip sample spanning every model, $k$ band
and outcome ships with each clip's transcript and scoring decision, so a
judgement can be checked by ear. Sampling uses each system's shipped defaults,
listed in S19; they make Qwen3-TTS the \emph{most} stochastic family, so its
larger checkpoint's null is not an artifact of conservative decoding.

\section{Results}
\label{sec:results}

% ---- placeholder: filled by analysis/make_numbers.py ----
\input{results_body}

\section{The Candidate Mechanism, Measured and Refuted}
\label{sec:theory}

The mechanism that motivated this study can be stated exactly, and we can now
report that it is false for these decoders. Attention over $k$ repeated spans
whose logits differ by at most $\delta$ gives each weight in
$[e^{-\delta}/k,e^{\delta}/k]$, so the profile flattens and the decoder meets
the same conditioning at every \emph{repetition boundary}, making the map $F$
between consecutive boundary states autonomous; if $F$ contracts with factor
$q<1$, no Lipschitz readout --- the stop head included --- separates counts
beyond $N^\ast=\log(\mu/2LC)/\log q$. Both are machine-checked in Lean~4 over
\texttt{mathlib}, free of \texttt{sorry} and with an axiom audit (S1--S2). The
implication holds; its antecedent does not.

\noindent\textbf{Measuring $q$.}
We estimate the top singular value of the boundary-to-boundary Jacobian by power
iteration with exact directional derivatives, teacher-forcing the generated
tokens so the map is well defined. The estimator passes three gates before being
pointed at the model: it recovers a synthetic Jacobian of known spectrum to
$4\times10^{-4}$; run backwards in time it returns \emph{exactly} zero, as a
causal transformer requires; and its Jacobian--vector products match finite
differences to $2\times10^{-5}$ where the map is linear. On Llasa-1B, over
\JacQN{} repeated items and their length-matched controls,
$q=\JacQRep{}$ $[\JacQRepLo{},\JacQRepHi{}]$ against \JacQCtl{}. \emph{The map
is not a contraction:} $q<1$ in \JacQNBelow{} of \JacQN{} items, in every lag
and sub-stack measured. Teacher-forcing removes the token channel, so this is a
\emph{lower} bound on the true Lipschitz constant, which makes $q\gg1$ decisive
rather than marginal; for the theorem to place $N^\ast$ where counts actually
saturate it would need $q\approx\JacQNeeded{}$.

The sign is wrong as well as the magnitude: repetition makes the map \emph{more}
expansive than its matched control, not less (paired $p=\JacQPairedP{}$,
repeated lower in \JacQNRepLower{} of \JacQN{} pairs). So does the dilution
lemma's dose-response --- within a $(\text{model},k)$ cell flatter attention
goes with \emph{better} counting ($r=\DilR{}$ $[\DilRLo{},\DilRHi{}]$; S12).
And one objection is structural: in a causal transformer a same-depth
single-vector state map is exactly zero, information reaching a later position
only by rising through depth, so the theorem's $s_m$ has no single-layer
realisation and only a whole-stack formulation is well posed (S5).

We keep the bound because it motivated the manipulation, and because a verified
conditional whose antecedent is measured and rejected is a cleaner object than
an untested one. It explains nothing in Section~\ref{sec:results}.

\section{Discussion}
\label{sec:discussion}

\input{discussion_body}

\vfill\pagebreak

\bibliographystyle{IEEEbib}
\bibliography{refs}

\end{document}
