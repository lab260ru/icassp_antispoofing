# Resolution record — Codex final audit

## Applied now

The following changes were made to `paper/main.tex` from the reviews without
changing a result, threshold, input, or paper scope:

1. Reframed the crest candidate as exploratory and discovery-frozen; changed
   `pre-registered` wording to repository-registered wording.
2. Separated completed contributions from the conditional H3 protocol.
3. Added the precise partial-rank adjustment, BH-family, cluster-bootstrap,
   full portability-rule, and released-score-artifact boundaries.
4. Added the passing polarity control to the H2 quality row and clarified the
   Figure 1 pass-count caption.
5. Reframed the conclusion as an association-portability audit with a path
   toward, rather than a completed, sensitivity test.

The required post-edit `tectonic` rebuild succeeded. The local static check
passes five US-letter pages, Table 1 on page 3, references on page 5, and
embedded-font checks. The rebuilt PDF is `paper/build/main.pdf`.

## Deliberately deferred

- No H5, H6, or S1 reference was added to the main paper. They remain sealed
  supplementary artifacts pending an authorized submission-package and archive
  decision.
- The current Figure 1 was not replaced with S1 because that would change the
  current self-contained package scope and requires an explicit archival/
  supplement decision, despite S1 being a stronger visual candidate.
- The complete corpus-by-control inventory and a permanent artifact locator
  require a submission-stage release decision; no locator is invented here.
- No new H1/H2/H2B/H3 experiment was started. The existing causal-quality stop
  rules remain binding.
