# V2 verification — evidence-boundary main figure

## Scope

Bounded Codex-only visual verification of
`fig_crest_evidence_boundary_main_v2.{pdf,png,metadata.json}` and the current
compiled manuscript. No analysis, values, caption text, or layout was edited
as part of this verification.

## Findings

- **H1 grouping is visibly corrected.** Panel A now has a dashed horizontal
  divider between the third and fourth fixed-order rows and explicit `locked
  discovery` / `held-out confirmation` labels. The plotted order remains
  ASV19 LA, ASV21 LA, ASV21 DF, InTheWild, ASV5; the two interval-bearing
  rows remain the held-out rows, as stated in the caption and metadata.
- **H2 scope is visibly corrected.** Panel B's title identifies an `ASV19 LA
  detector-free quality screen`, and its panel annotation says `1,000-clip
  panel; no score-eligible manifest frozen`. The caption independently states
  `1,000-clip ASVspoof2019 LA quality panel` and that no score-eligible
  retained-pair manifest was frozen, hence no detector scoring occurred.
- **No layout/caption regression observed.** The full-width vector graphic is
  readable in its 300-dpi preview; headings, divider labels, axes, percentage
  labels, and the 90% gate do not overlap. The neutral polarity colour avoids
  treating the control arm as an affirmative detector result. The compiled PDF
  remains five US-letter pages, with Fig. 1 and its complete caveated caption
  on technical page 4 and references on page 5; the local PDF preflight
  passes.

## Verdict

**Pass.** Both requested narrative corrections are plainly visible and the
updated figure/caption preserves the conservative no-score/no-causal boundary
without introducing a page-budget or legibility issue.
