# Resolution record — reviewed main-figure replacement

The v1 two-panel evidence-boundary asset was preserved. Review comments led to
the separately versioned v2 layout contract, not an overwrite or a scientific
reanalysis. The current `paper/main.tex` points only to v2.

Applied changes:

1. H1 now shows a fixed discovery/held-out divider and labels.
2. H2 now names ASVspoof2019 LA and its 1,000-clip detector-free scope.
3. The gate explanation identifies the missing **score-eligible retained-pair
   manifest**, rather than suggesting the frozen input/arm design was absent.
4. Polarity is neutral-coded and its individual arm pass is explained as
   insufficient to open the failed four-arm panel.
5. The caption says `fixed-analysis`, names the fixed model-level portability
   condition, and rejects cross-panel inference explicitly.

The v2 visual verification passes, and `paper/main.tex` was immediately
compiled. The resulting `paper/build/main.pdf` remains a readable five-page
working draft; no causal claim was added.
