# Codex methods review

Scope: read-only review of the current paper source and compiled PDF against
the locked H1/H2 artifacts.

The reviewer found no contradiction in the reported H1/H2 numbers and no
unsupported causal result. The central required correction was that the crest
follow-up must not be called confirmatory: its candidate rule was introduced
during the discovery loop. The manuscript now calls H2 a planned quality-gated
sensitivity study and explicitly labels the instantiated crest follow-up as
exploratory.

The reviewer also identified four precision issues. All were addressed in the
source: the quality gate now includes the registered target-feature direction;
the feature-label AUROC and model/class-specific association granularity are
separated; the 2,000 clustered bootstrap is limited to the frozen manifest
candidate in held-out confirmation; and the central Table 1 row now reports
the two held-out adjusted q values. The reviewer suggested adding the two
confidence intervals if space permits; the compact table retains the q values
and the intervals remain in the hash-sealed result artifacts.
