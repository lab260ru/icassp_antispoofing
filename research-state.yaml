# Central state for the AR-TTS counting-collapse study.
# A fresh agent should be able to resume from THIS FILE + findings.md + README.md.
project: "Counting Collapse in Autoregressive TTS"
venue: "ICASSP 2026 (4 pages + 1 page refs)"
started: "2026-08-11"
branch: "research/ar-tts-counting-collapse"
remote: "https://github.com/lab260ru/icassp_antispoofing"

question: >
  Autoregressive TTS models mis-render text containing many repetitions
  (looping, truncating, losing count). Is this a structural consequence of the
  decoder's state dynamics under periodic conditioning rather than a sampling
  artifact? If the per-repetition state map contracts, the model provably
  cannot count past a finite horizon.

phase: "P4-ship"   # P0 scaffold, P1 bringup, P2 sweep, P3 refinement, P4 ship

# ---------------------------------------------------------------- what is done
settled:
  lean: >
    COMPLETE and verified. 11 theorems in lean/SpectralTTS, zero sorry, axiom
    audit shows only propext / Classical.choice / Quot.sound. Gate:
    scripts/check_lean.sh. Do not re-derive; it is finished.
  theory: >
    Theorem A (i)-(iv) counting collapse + Lemma B attention dilution. The Lean
    artifact proves an IMPLICATION; the contraction premise is empirical. Keep
    that distinction explicit in all writing -- reviewers rewarded it.
  benchmark: >
    180 stimuli, data/stimuli/stimuli.jsonl, regenerable by make_stimuli.py.
    Regenerating only changes metadata, never text, so existing generations
    stay valid.
  behavioural_pipeline: "generate -> vocode (Llasa only) -> Whisper -> score_counts"

# --------------------------------------------------------------- key findings
# ALL numbers below are CTC-judged on the population defined by
# src/common/population.py (ablations, degenerate audio, judge-unmeasurable
# template t2, and items truncated by our own token budget all excluded).
# Whisper-judged numbers from before 2026-08-12 are superseded; see findings.md.
findings_headline:
  dissociation: >
    Repeated text is miscounted where length-matched controls are not. At k>=6:
    repeated median relative error -8.3% [-12.5,-6.2] n=457, control 0.0
    [0.0,0.0] n=525. Exact-rate is the sharper statement: 94.3% of control
    generations come back exactly right against 18.2% of repeated ones.
    Separated from own control in 4 of 6 checkpoints.
  horizon: >
    THE MAIN LADDER WAS TOO SHORT. Within k<=32 the count is proportional
    (c = 0.95k, SSE 2.6 against 485 for a constant), so no horizon is visible.
    The extension ladder k=48..128 finds it: repeated medians fall 27 -> 15
    between k=48 and 128. Controls saturate too, near 60 -- these decoders have
    a general utterance-length ceiling -- so the reportable quantity is the
    RATIO: N*_rep 23 [16,39] vs N*_ctl 94 [71,111], disjoint in both
    checkpoints, i.e. repetition lowers the counting horizon ~4x.
    NEVER quote a repeated horizon without its matched control horizon.
  mechanism: >
    Capacity gain dN_eff/dlog k on repeated text is 0.46x the control gain,
    disjoint 95% template-bootstrap CIs in 5 of 6. Crucially the gain stays
    POSITIVE in 5 of 6: repetition slows state acquisition, it does not halt it,
    which is consistent with decoders below their horizon at k<=32.
  lemma_b: >
    Renormalised over the k repeated keys, block entropy grows with log k. The
    entropy gap bounds the AVERAGE spread (<=0.45 nats); the most-attended
    occurrence bounds the EXTREME (delta <= 1.23, share <= 3.4/k worst case,
    1.7/k typical). The lemma needs the extreme. Do not quote the average as
    though it were the bound -- an earlier draft did, and it read as inconsistent.
  negative_results: >
    (1) The boundary-difference estimator for q is unsound here: deep-layer
    attention centroid advances on ~51% of steps. (2) A finite-difference
    perturbation probe (analysis/contraction_probe.py) found NO difference in
    local contraction between repeated items and controls -- 7 of 18 pairs in
    the predicted direction, p=0.12, replicated at two injection depths, with
    its own linearity control failing. Assumption 2 (contraction) is therefore
    UNESTABLISHED after two independent attempts. Say so; do not present either
    q-hat as validated.
  nonar_baseline: >
    VITS (facebook/mms-tts-eng), src/models/vits_gen.py, run on the same ladder
    shows NO dissociation: 65.6% of repeated items exact against 58.9% of
    controls, gap -6.7 pts where the AR panel's is +76.1, and all 6 AR
    checkpoints separate from it. It degrades with length on both families
    alike. This rules out "long repeated text is just hard" AND clears the CTC
    judge -- blank-collapse would have depressed VITS's repeated items too.
    Run this before trusting any judge result on repeated speech.
  judge: >
    CTC (wav2vec2-large-960h-lv60-self), never Whisper. On real generated audio
    Whisper emits 10.7-12.3 words/sec at k>=24, which is not physically speech.
    CTC's own limit: blank-collapse can merge adjacent identical words, which
    depresses repeated counts but not control counts -- it inflates our reported
    gap rather than manufacturing it, so magnitudes at k>=16 are approximate.

model_panel:
  - {key: llasa1b, status: complete, ext_ladder: complete}
  - {key: llasa3b, status: complete, ext_ladder: "not run"}
  - {key: llasa8b, status: complete, ext_ladder: complete}
  - {key: xtts2,   status: complete,
     ext_ladder: "IMPOSSIBLE -- built-in ~602 mel-token ceiling (~26 s) censors every k>=48 item"}
  - {key: qwen06b, status: complete, ext_ladder: generating}
  - {key: qwen17b, status: complete, ext_ladder: generating}
  - {key: "xtts2norp / xtts2rp{2,3,8}", status: complete,
     note: "ablation arms, repetition penalty sweep. NEVER pool into panel stats."}
instrumentation_notes:
  qwen: "hidden states yes, text attention NOT obtainable (text+codec embeddings additively fused)"
  xtts: "full; note shipped repetition_penalty=5.0 -> conservative panel member"
  llasa: "full; SDPA for sampling, eager only for the instrumented pass"

# ---------------------------------------------------- what has been excluded
# Six rival explanations for the periodicity-specific deficit, each with the
# analysis that killed it. A new agent should not re-litigate these.
rivals_excluded:
  length: "matched controls at the same word count come back exact; analysis/count_error.py"
  control_periodicity: >
    main-ladder controls cycle an 8-word pool above k=8. Re-generated k=12..32
    from a never-cycled 146-word pool: gap +71.2 vs +84.6 pts, so the confound is
    worth 13.4 points. analysis/aperiodic_controls.py
  repetition_penalty: >
    Llasa passes NO penalty and shows the effect at full size (14.0 vs 92.9);
    XTTS-v2 across a 4x penalty range holds at 15.6-21.1%.
    analysis/penalty_confound.py
  asr_judge: >
    A non-AR model through the SAME judge on the SAME strings shows no gap.
    Also: two recognisers disagree by 0.47 counts and ours reports the HIGHER
    count in 11/13 disagreements, so it under-states the deficit.
    analysis/nonar_baseline.py, analysis/noise_floor.py
  architecture: >
    NOT EXCLUDED -- withdrawn 2026-08-12. Two non-AR baselines disagree: VITS
    shows no dissociation (-6.7) but F5-TTS shows +60.0, growing with k as the
    panel's does. Absence of an autoregressive decoder confers no immunity. The
    replacement hypothesis (post-hoc, untested): what matters is whether the
    count must be held internally, not recurrence. analysis/nonar_baseline.py
  improbability_and_acoustics: "ruled out by their own predictions; supplement S8"

# ------------------------------------------------------------------- in flight
active_background_jobs:
  - "llasa1b/llasa8b extension ladder seeds 1,2 (GPU 0,1) -> experiments/exp08_ext_ladder/logs/seeds12.log"
  - "qwen06b/qwen17b extension ladder seed 0 (GPU 2,3) -> scripts/run_ext_ladder.sh"

# --------------------------------------------------------------- what is next
next_actions:
  - "extension ladder COMPLETE: 4 checkpoints, 2 families, 21-26 items/cell"
  - "THEOREM'S CONCLUSION now has one direct test that passes (probe past the
     horizon, analysis/probe_past_horizon.py). The PREMISE is still unsupported:"
  - "MECHANISM IS UNSUPPORTED: contraction unmeasured (2 attempts), dilution
     dose-response sign-reversed, probe non-discriminating. Do not write a
     mechanistic claim without reading findings.md first."
  - "title was rescoped after round 6; do not restore the attractor-theory framing"
  - "paper is at 5 pages (4 content + 1 refs) and must stay there -- check every build"
  - "final: check_lean.sh, build both PDFs, tag v1.0-submission"

review_round_1:
  location: "paper/reviews/r1/"
  addressed: >
    stale abstract claim, method/results mismatch, table caption, audit-bucket
    framing, broken citations, internal path in a bib note, outcome taxonomy,
    LaTeX escape in a caption, code availability, author block.
  outstanding:
    - "panel breadth: fixed as models land"
    - "naturalness/perplexity confound not separately controlled (controls at high k are also unnatural word salad -- state this)"
    - "acoustic/codec-token repetition attractor as an alternative locus: not ruled out"
    - "Lemma B and Theorem A not experimentally separated"

final_day_results:
  probe_past_horizon:
    status: "1 of 3 checkpoints; llasa1b loses the count, llasa3b and llasa8b do not"
    caution: >
      llasa3b's MAE ratio is 0.99 -- on the 'keeps the count' side of the
      threshold by 0.006 over 12 items, which is not a margin. By effect size
      2 of 3 show nothing recoverable. The paper quotes 1 of 3, the less
      favourable reading; S12 records both.
    range_confound: "ruled out by a matched control arm over the same k"
    script: analysis/probe_past_horizon.py
  greedy_decoding:
    status: "deficit survives argmax: gap 72.2 vs 75.0 sampled, cap-hit 0% both arms"
    script: analysis/greedy_decoding.py
  sample_sizes:
    status: "5400 -> 2052 -> 1559 -> 982 = 457 + 525, reconciled and asserted"
    script: analysis/sample_sizes.py
  horizon_ratio:
    status: "direction form-independent, magnitude form-dependent (2.7-4.3)"
    script: analysis/horizon_forms.py
  exclusion_sensitivity:
    status: "gap 61.7 with every rule off vs 76.1 as reported"
    script: analysis/exclusion_sensitivity.py
  vits_config:
    status: "immunity holds at 3 duration-predictor settings; sign not size"
    script: analysis/vits_config.py
  model_revisions:
    status: "10 repositories pinned incl. judge and vocoder"
    file: data/results/model_revisions.json
